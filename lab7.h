#ifndef _LAB7_LAB_
#define _LAB7_LAB_

#include <iostream>
#include <sstream>
#include <istream>
#include <fstream>
#include <utility>
#include <istream>
#include <fstream>
#include <algorithm>
#include <iterator>
#include <string>
#include <vector>
#include <cmath>
#include <regex>

#include "bigint.h"

std::pair<bigint, bigint> myproduct(std::pair<bigint, bigint>, std::pair<bigint, bigint>); // takes two fractions of bigints and returns fraction

std::pair<bigint, bigint> mysum(std::pair<bigint, bigint>, std::pair<bigint, bigint>); 

#endif